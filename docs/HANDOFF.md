# AgentCal Technical Handoff

## Completed
Cursor applied a WT-reviewed Feature 02 information-architecture fix on `feature/02-tier2-export-share` at `C:\dev\agentcal`. This is not a new feature and not a copy rewrite: `lib/engine/` and Feature 01 calculation logic were not touched. Feature 01's View calculation dialog is unchanged. Copied/emailed text remains self-contained.

## Changed
- `app/estimate-summary.tsx` — the always-visible "Email content" textarea is now behind a collapsed-by-default accordion (`data-testid="estimate-email-toggle"`), matching Feature 01's `accordion-trigger` / `aria-expanded` / `aria-controls` / local `IconChevron` pattern. Trigger label: "Edit email text before copying" / "Hide email text". Expanding reveals the same textarea (`data-testid="estimate-email-content"`). Copy/mailto still use the full composed text whether or not the editor is expanded. The paste caption and copy-status stay visible regardless of expand state. The article's Tier 2 `<pre>` now calls `composeTier2Disclosure({ …, includePreparedByLine: false })`. The top-of-article `composePreparedByLine` under the title is unchanged.
- `lib/sell-narration.ts` — `composeTier2Disclosure` accepts optional `includePreparedByLine` (default `true`). When false, the prepared-by/date lines are omitted; assumptions summary + referral remain. `composeClipboardHandoff` and `composeMailtoSummary` keep the default (`true`) so handoff text stays self-contained.
- `lib/sell-narration.test.ts` — added coverage that `includePreparedByLine: false` drops prepared-by/date while keeping assumptions/referral; default `true` still includes them. Existing clipboard/mailto tests still assert the default self-contained handoff with one closing disclaimer pair.
- `e2e/estimate-summary.spec.ts` — default load asserts the email editor is collapsed (`aria-expanded="false"`, textarea hidden) and `estimate-artifact` contains `"Prepared by"` exactly once. Tests expand the editor before reading `estimate-email-content`. Print path also asserts `"Prepared by"` once. Copy-while-collapsed still produces the full self-contained body including `"Prepared by:"`. Keyboard coverage reaches the new toggle, then the textarea after expand.

## Verified
Exact commands in `C:\dev\agentcal` on 2026-08-20:

1. `pnpm lint` — success (exit 0).
2. `pnpm type-check` — success (exit 0).
3. `pnpm test` — **38 passed**, 0 failed (26 existing SELL engine tests + 12 narration/handoff tests).
4. `pnpm build` — success (Next.js 16.3.0). Routes: `/`, `/_not-found`.
5. `pnpm test:e2e` — **32 passed**, 0 failed (19 existing Feature 01 consultation tests + 13 Feature 02 tests).

Print artifacts were regenerated by the e2e print-preview test:

- `test-results/estimate-summary-print.pdf`
- `test-results/estimate-summary-print-preview-1280.png`

(gitignored; re-run `pnpm test:e2e` to refresh.)

IA contract now verified:

- Default on-screen page (editor collapsed): narration, Tier 1/tax disclaimer, and Tier 2 assumptions+referral appear once each, in the printed article only.
- Expanding the editor shows the same composed text a second time inside the textarea by design.
- Printed article: `"Prepared by"` appears once, at the top. `TIER1_DISCLAIMER` / `TAX_EXCLUSION_NOTE` appear once in Disclosure. Tier 2 assumptions/referral appear once in the Tier 2 block (no prepared-by/date there).
- Clipboard/mailto text, taken in isolation: still self-contained — narration, Tier 2 block WITH prepared-by/date, and the closing disclaimer pair.

## Open
- Work remains **uncommitted and unpushed** on `feature/02-tier2-export-share` (this IA fix plus the earlier copy-refinement already in the working tree). Commit/push/PR remain human gates.
- Claude independent review of this IA-fix diff and verification evidence is required next. Cursor has stopped editing.
- Tier 2 disclaimer wording and the §4a narration templates remain pending managing-broker / BC counsel review (Charter §15). This cycle only collapsed the email editor and removed the article's duplicate prepared-by/date line; it did not rewrite locked disclaimer sentences.
- M1 still requires Feature 02 to be independently reviewed, human-accepted, deployed, and production-verified.

## Current Tool Arrangement
Cursor has **stopped editing**. Control is handed back to Claude for independent review of the actual diff and evidence. Do not have Claude and Cursor edit the same working tree concurrently.

## Next
1. Claude independently reviews this Feature 02 information-architecture fix on `feature/02-tier2-export-share` at `C:\dev\agentcal` against Product Spec §13, WT's duplication notes, and this evidence — including visual AC-06 re-check of the regenerated print PDF/screenshot.
2. Human gate: WT acceptance, then commit/push/PR when requested.

## Stop / Escalation Conditions
Stop if Product Truth conflicts with this implementation, another coding agent starts editing `C:\dev\agentcal`, Feature 01 calculation behaviour is asked to change, or a merge/release is requested without WT acceptance.
