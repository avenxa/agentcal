# AgentCal Technical Handoff

## Completed
Cursor applied a WT-reviewed Feature 02 copy refinement on `feature/02-tier2-export-share` at `C:\dev\agentcal`. This is not a new feature: `lib/engine/` and Feature 01 calculation logic were not touched. Feature 01's own View calculation dialog still renders both disclosure captions by default.

## Changed
- `lib/sell-copy.ts` — `EMAIL_PASTE_PROMPT` shortened to `"Copied. Paste this into a new email in your own mail client."` (dropped the client-email-address reassurance).
- `app/estimate-summary.tsx` — matching chrome caption shortened the same way. Feature 02's `BreakdownBody` embed now passes `hideDisclosure`. The artifact Disclosure section remains the single on-screen/print home for `TIER1_DISCLAIMER` and `TAX_EXCLUSION_NOTE`, then the existing Tier 2 block.
- `app/view-calculation.tsx` — added optional `hideDisclosure?: boolean` (default `false`) on `BreakdownBody`. Feature 01's `ViewCalculationDialog` call site is unchanged.
- `lib/sell-narration.ts` — `composeResultNarration()` no longer appends the disclaimer pair. `composeTier2Disclosure()` no longer trails with `TIER1_DISCLAIMER`. `composeClipboardHandoff()` and `composeMailtoSummary()` now place the disclaimer pair exactly once at the very end, after the Tier 2 referral content.
- `lib/sell-narration.test.ts` — narration assertions no longer expect inline disclaimers. Added exact-count and end-position regression coverage for clipboard and mailto (fails if either disclaimer string count is 0 or >1, or if the pair is not last).
- `e2e/estimate-summary.spec.ts` — narration must not carry the pair; on-screen and print artifacts must contain each string exactly once; clipboard/mailto bodies must carry the pair once at the end; client-email reassurance copy is gone.

## Verified
Exact commands in `C:\dev\agentcal` on 2026-08-20:

1. `pnpm lint` — success (exit 0).
2. `pnpm type-check` — success (exit 0).
3. `pnpm test` — **37 passed**, 0 failed (26 existing SELL engine tests + 11 narration/handoff tests).
4. `pnpm build` — success (Next.js 16.3.0). Routes: `/`, `/_not-found`.
5. `pnpm test:e2e` — **32 passed**, 0 failed (19 existing Feature 01 consultation tests + 13 Feature 02 tests).

Print artifacts were regenerated by the e2e print-preview test:

- `test-results/estimate-summary-print.pdf`
- `test-results/estimate-summary-print-preview-1280.png`

(gitignored; re-run `pnpm test:e2e` to refresh.)

Disclaimer contract now verified by exact string count, not only visual spot-check:

- On-screen and printed artifact: each of `TIER1_DISCLAIMER` and `TAX_EXCLUSION_NOTE` appears once, in the closing Disclosure section (not in narration, not in the Feature 02 breakdown footer).
- Clipboard/email content and mailto summary: the same pair appears once and is the last two lines, after the Tier 2 referral content.

## Open
- Work remains **uncommitted and unpushed** on `feature/02-tier2-export-share`. Commit/push/PR remain human gates.
- Claude independent review of this copy-refinement diff and verification evidence is required next. Cursor has stopped editing.
- Tier 2 disclaimer wording and the §4a narration templates remain pending managing-broker / BC counsel review (Charter §15). This cycle only removed duplicate renderings and shortened the email paste prompt; it did not rewrite the locked disclaimer sentences.
- M1 still requires Feature 02 to be independently reviewed, human-accepted, deployed, and production-verified.

## Current Tool Arrangement
Cursor has **stopped editing**. Control is handed back to Claude for independent review of the actual diff and evidence. Do not have Claude and Cursor edit the same working tree concurrently.

## Next
1. Claude independently reviews this Feature 02 copy-refinement on `feature/02-tier2-export-share` at `C:\dev\agentcal` against Product Spec §13, the WT copy notes, and this evidence — including visual AC-06 re-check of the regenerated print PDF/screenshot.
2. Human gate: WT acceptance, then commit/push/PR when requested.

## Stop / Escalation Conditions
Stop if Product Truth conflicts with this implementation, another coding agent starts editing `C:\dev\agentcal`, Feature 01 calculation behaviour is asked to change, or a merge/release is requested without WT acceptance.
